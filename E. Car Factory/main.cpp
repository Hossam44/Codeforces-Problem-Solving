#include <bits/stdc++.h>

using namespace std;
typedef long long ll;
int main()
{
    ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0);
    ll t,n,a;
    cin>>t;
    while(t--)
    {
        cin>>n>>a;
        cout<<a+n-1<<endl;

    }
    return 0;
}
